fastapi
uvicorn
sqlalchemy
pydantic
pydantic-settings
httpx
python-dotenv
psycopg[binary]
celery[redis]
redis
llama-cpp-python
PyJWT
transformers
torch
pillow
python-multipart
pytest
# Optional HTR backend (recognition + fine-tuning), install to enable it.
# Windows note: coremltools has no Windows wheels, so `pip install kraken` fails;
# install kraken with --no-deps plus the deps listed in app/htr/README.md.
# kraken
